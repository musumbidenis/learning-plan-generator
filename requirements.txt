streamlit>=1.40
pdfplumber>=0.11
pymupdf>=1.24        # fast PDF word extraction (fitz); pdfplumber is the fallback
python-docx>=1.1
olefile>=0.46        # read legacy Word 97-2003 (.doc) binaries
python-dotenv>=1.0
requests>=2.31
pytest>=7.0          # dev/test only
